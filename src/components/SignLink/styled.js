import styled from "styled-components";

export const SignSpan = styled.span`
    font-size: 14px;
    font-weight: 400;
    color: #52B6FF;

    text-decoration-line: underline;

    &:visited, &:active{
        color: #52B6FF;
    }
`;